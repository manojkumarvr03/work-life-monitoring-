{
  "name": "work-life-monitor-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-frontend": "cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  }
}
